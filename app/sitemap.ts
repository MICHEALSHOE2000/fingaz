import type {MetadataRoute} from 'next';import {articles,categories,products,solutions} from '@/lib/data';export default function sitemap():MetadataRoute.Sitemap{const base='https://fingazconcept.com';const staticRoutes=['','/products','/solutions','/brands','/about','/resources','/contact','/request-quote','/compare'];return [...staticRoutes.map(r=>({url:base+r})),...Object.keys(categories).map(s=>({url:`${base}/category/${s}`})),...products.map(p=>({url:`${base}/products/${p.slug}`})),...solutions.map(s=>({url:`${base}/solutions/${s.slug}`})),...articles.map(a=>({url:`${base}/resources/${a.slug}`}))]}
