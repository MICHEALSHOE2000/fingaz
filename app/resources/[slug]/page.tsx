import {notFound} from 'next/navigation';import Link from 'next/link';import {articles,getArticle} from '@/lib/data';export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}export default function Page({params}:{params:{slug:string}}){const a=getArticle(params.slug);if(!a)notFound();return <main className="section"><article className="container"><p><Link href="/resources">Resources</Link> / {a.title}</p><h1>{a.title}</h1><p className="muted">{a.description}</p>{a.body.map(p=><p key={p}>{p}</p>)}</article></main>}
