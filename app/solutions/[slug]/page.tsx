import {notFound} from 'next/navigation';import Link from 'next/link';import {CTA} from '@/components/ui';import {solutions} from '@/lib/data';export function generateStaticParams(){return solutions.map(s=>({slug:s.slug}))}export default function Page({params}:{params:{slug:string}}){const s=solutions.find(x=>x.slug===params.slug);if(!s)notFound();return <main className="section"><div className="container"><p><Link href="/solutions">Solutions</Link> / {s.title}</p><h1>{s.title}</h1><p className="lead" style={{color:'#5f6b7a'}}>{s.summary}</p><div className="grid grid3">{s.points.map(p=><div className="card" key={p}>{p}</div>)}</div></div><CTA/></main>}
