import {notFound} from 'next/navigation';import {ProductGrid} from '@/components/ui';import {categories,products,CategorySlug} from '@/lib/data';export function generateStaticParams(){return Object.keys(categories).map(slug=>({slug}))}export default function Page({params}:{params:{slug:string}}){if(!(params.slug in categories))notFound();const slug=params.slug as CategorySlug;return <main className="section"><div className="container"><h1>{categories[slug]}</h1><p className="muted">Products and project recommendations for {categories[slug].toLowerCase()}.</p><ProductGrid items={products.filter(p=>p.category===slug)}/></div></main>}
