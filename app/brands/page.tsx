import type {Metadata} from 'next';
import Link from 'next/link';
import {ArrowRight} from 'lucide-react';
import {brands,products} from '@/lib/data';
export const metadata:Metadata={title:'Networking & Security Brands',description:'Browse Ubiquiti, MikroTik, Cisco, TP-Link, Hikvision and D-Link products available through Fingaz in Lagos, Nigeria.',alternates:{canonical:'/brands'}};
export default function Page(){return <main><section className="pageHero"><div className="container"><p className="overline">Brands</p><h1>Infrastructure From<br/>Recognised Manufacturers.</h1><p>Browse products by brand and request current pricing or project availability. Fingaz does not claim official distributorship unless explicitly stated.</p></div></section><section className="section"><div className="container"><div className="productGrid">{brands.map(brand=>{const count=products.filter(p=>p.brand===brand).length;return <Link className="productCard" href={`/brands/${brand.toLowerCase().replace(/[^a-z0-9]+/g,'-')}`} key={brand}><div className="productVisual"><span className="productBrand">{brand}</span><span className="productType">Networking / infrastructure</span></div><div className="productBody"><div className="productMeta"><span>{count} catalogue item{count===1?'':'s'}</span></div><h3>Shop {brand} products from Fingaz</h3><p className="muted">Explore available product categories and request a current quotation for your requirement.</p><span className="textLink">Explore brand <ArrowRight size={14}/></span></div></Link>})}</div></div></section></main>}
